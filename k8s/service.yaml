apiVersion: v1
kind: Service
metadata:
  name: configmap-secret-service
spec:
  type: NodePort
  selector:
    app: configmap-secret-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
